# STL web simulator — Python runtime + test dependencies (backend-core owns this file;
# other packages append their own lines below, one requirement per line).
numpy>=1.26
scipy>=1.11
numba>=0.59
fastapi>=0.110
uvicorn[standard]>=0.29
orjson>=3.9
pytest>=8
httpx>=0.27
