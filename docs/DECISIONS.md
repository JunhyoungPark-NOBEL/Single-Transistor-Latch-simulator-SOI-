# Implementation decisions (append-only)

- [foundation] Engine copied verbatim from the handoff zip into `engine/`; runtime caches are gitignored.
- [foundation] `server/params.py` reads every calibrated number from the engine JSON files (single source of truth); `build_p()` reproduces `setup_photo.BASE` exactly for the paper preset and `S.params(...)` for the photo preset (checked to 1e-27).
- [foundation] Photo preset applies the body-coupling option γ = 0.2794 (p[15]) as `mc_cycles.py` does; the high-V_D seed (p[17], p[18]) stays an alternative option in the UI (open problem).
- [stochastic] Hazard curves generalise `photo_fpt.hazard_curve` to the full 26-element p (same voltages/u grid/lattice; classify grid = device.numerics.grid, default 601); window 0.28 V is extended to 0.5/0.8 V only when the escape probability in the window's first 10 mV exceeds 1e-3 at the requested rate (gate_fpt uses 0.5 V near V_G = -1.1 V). Cross-check VG -2 V dark 0.4 V/s: 3.6442 V / 8.03 mV.
- [stochastic] Disk cache `server/.cache/stochastic/{fold,hazard}/<sha256>.json`, key = sha256(CACHE_VERSION, p formatted '%.12g', algorithm settings); one file per fold node / hazard node so sweep_mc, hazard and vg_curve_stochastic share work; atomic writes (tmp + os.replace) for concurrent workers.
- [stochastic] State mapping (general engine): gidl X = extra dg on p[9] (centre 0, build_p already holds δφ_G0); local_avalanche X = p[23] (centre ext.dloc, p[21] forced to 1.0 with a warning when ext.aloc <= 0); junction X = p[19]; multiplication X = p[20]. cycle_state and fold_table.delta are X - centre; absolute centre in the extra key state_axis.
- [stochastic] Fold table: classify at X0 + σ·linspace(-4.5, 4.5, fold_nodes), PCHIP over nodes with a latch, evaluated through a 4001-point fine grid; states whose nearest node has no latch (or outside the valid node range) are 'no latch' -> that cycle is censored (mc_cycles.py clips instead; the fraction is reported as no_latch_weight + warning).
- [stochastic] Emitter state: linear fold shifts dV_LU/dE and dV_LD/dE from classify at E = ±σ_E around the centre state (both applied).
- [stochastic] Carrier noise: LU hazard nodes at Gauss-Hermite states X0 + σ t_k (hazard_nodes), log hazard tabulated on a 1 mV distance-to-fold grid, bilinear in (X, distance) in log space, clamped to the end nodes outside their range, held constant between the last computed voltage and the fold (as gate_state_lookup.npz), zero beyond the window. LD hazard only at the centre state vs distance above the LD fold.
- [stochastic] MC time stepping: triangular sweep, steps+1 points per half (dt = dv/rate), trapezoidal ∫h dt vs an Exp(1) threshold; the event voltage is linearly interpolated inside the step (continuous values, no 2 mV quantisation); fold crossing interpolated likewise. Random numbers: Z, U, Z_E, U_LD drawn up front from default_rng(seed) (Z, U in mc_cycles.py order = common random numbers); OU innovations from default_rng([seed, 11]) / ([seed, 12]), exact AR(1) discretisation, continuous across cycles and chunks.
- [stochastic] Latch-down is paired with latch-up: a cycle that never latched up (censored V_LU) has V_LD = null (gate_dynamic_compare treats up/down unpaired).
- [stochastic] Calibrated engine: V_LU/V_LD are gate_dynamic_compare's 10 mV read-out midpoints (VALIDATION numbers: 3.634 V/119 mV, 2.700 V/21.4 mV); continuous values in extra keys V_LU_continuous/V_LD_continuous. cycle_state comes from an exact replay of simulate()'s random stream (verified bit-identical events in the tests). auto also requires carrier_noise = true (the lookup always contains LU and LD first-passage noise).
- [stochastic] vg_curve_stochastic: fold table on the same nodes as sweep_mc (X0 + σ·linspace(-4.5,4.5,fold_nodes), cache-shared), PCHIP onto vg_sweep.py's 81-point grid t ∈ [-4, 4] with Gaussian weights; FPT noise SD and mean shift from photo_fpt.quantiles at hazard_nodes GH states, added in quadrature; evolving states use their stationary distribution; σ_E ignored (moves V_LD); latch window by bisection (2.5 mV) of the centre-state latch existence (extra key `window`).
- [physics-B] Topics stochastic-events, first-passage, local-states, sweep-mc, circuit-element, design-map, open-problems, validation live in `web/src/content/physics/topics/<id>.ts`; shared helper `topics/_sharedB.ts` has no default export, so the registry glob ignores it. Every equation carries a `code` reference; numbers quoted were re-run (FPT node 3.6442 V / 8.03 mV, dynamic MC 3.6344 V / 119.0 mV, 2.6999 V / 21.4 mV, 10×100 pooled 125.8 / 19.6 mV with seeds 2026093000+k, fold sensitivities −0.796 and −39.8 V/V).
- [physics-B] design-map relations are *inferred* from the arrays (generator not in the handoff) and labelled so in the UI; each matches to machine precision: N̄ = N_t·L0², σ_φ = (1−d)·q·√N_t / (2·C'_ox·L0) with C'_ox = 3.9ε0/EOT, σ_VLU and latched_fraction = PCHIP of data/fill_response_sweep4V.json (latched = switched/1000), φ_50mV = PCHIP root at 50 mV, iso-lines at d = 0. sigma_VLU_sweep5p2V_mV is shown as given.
- [physics-B] Inconsistencies documented in the content (code untouched): photo_mean p[24] = 2 ("edge without channel") is unreachable because `bulk = p[24] > 0.5` is tested first (p[24] = 2 ≡ 1, verified); stl_api.py smoke text says FPT SD "6.8 mV" (code 8.03, VALIDATION ≈ 8) and photo V_LD "2.5959" (code 2.5962); dV_LD/dφ_E is −39.8 V/V for the fold and −41.2 V/V for the lookup escape mean; MODEL_SPEC §1 / data/tables `Q_B` is the excess charge only, not the lattice/circuit coordinate C_ox ψ + Q_exc + qN_A A L_n.
- [physics-B] Cross-package observations (for the owners, not changed): validation.py `check_paper_records` uses seeds 2026092920+k (gives 119.6 / 18.9 mV) although gate_dynamic_compare.main() uses 2026093000+k (reproduces 125.8 / 19.6 mV exactly); the general sweep engine with `ld_carrier_noise: false` (preset default) puts V_LD at the fold (2.599 V vs 2.7007 V with it on, measured 2.7001 V).
- [physics-A] Topics overview, electrostatics, impact-ionization, btbt-gidl, channel, bjt-transport, charge-balance, photo, parameters, numerics are written as plain `PhysicsTopic` objects (one file each, TeX in `String.raw` literals where possible, no shared helper file). Every quoted number was recomputed against the engine (`stl_api`, grid 601) on 2026-09-24.
- [physics-A] Code finding (engine left untouched): in `photo_mean.components()` `bulk = p[24] > 0.5` is also true for p[24] = 2, so the "edge excluding channel" local-path definition (`edge_only`) is unreachable; p[24] = 2 gives outputs identical to p[24] = 1 (verified). Documented in impact-ionization/parameters; UI/backends should not present 2 as a distinct option without a wrapper fix.
- [physics-A] Three charge definitions coexist and are documented as such in charge-balance: z[13] = C_ox·u + Q_exc (components), Q_B = Q_exc (export_tables / MODEL_SPEC §1, §5), Q = C_ox·ψ + Q_exc + q·N_A·A·L_n (compound_fpt lattice, WEB_CONTRACT circuit element). The quasi-potential U(x) is described exactly as WEB_CONTRACT §2 states it (backend-defined).
- [physics-A] MODEL_SPEC vs code differences recorded in the topics: V_D relation omits R_c (code uses (R_c + R_acc)·I_D); photo-electrons are multiplied by max(M−1,0) without the channel-II scale p[12] (spec says "like channel electrons"); I_PH enters the transport solver only via the collector-side boundary source b, not as distributed generation.
- [stochastic] Local caps mirror server/payloads.py (defensive for direct calls) except dv_V >= 0.5 mV (MC cost; clamped with a warning); calibrated_lookup requested explicitly for a non-reference device / action / vd_max != 4 raises ValueError instead of silently switching engines. Node cache dir overridable with env STL_STOCH_CACHE_DIR.
- [backend-core] Jobs run in a `spawn` ProcessPoolExecutor; progress and cancel flags travel through two `multiprocessing.Manager` dicts; the worker-side progress callback writes at most every 0.2 s and raises `JobCancelled` when flagged. DELETE reports `cancelled` immediately (the worker stops at its next progress call; a result that still completes is cached but not attached).
- [backend-core] Results are serialised to JSON bytes inside the worker (orjson, NaN/inf → null) and embedded with `orjson.Fragment`; endpoints return explicit Response objects (FastAPI's `jsonable_encoder` is bypassed).
- [backend-core] Cache key = sha256(kind, canonical JSON of the *normalised* payload (sorted keys, ints as floats), clamp warnings, ENGINE_VERSION); ENGINE_VERSION also hashes `server/engine_bridge.py`. Identical in-flight payloads are de-duplicated to the running job.
- [backend-core] The API process normalises payloads before submission (`server/payloads.py`): device/sweep/stochastic blocks are resolved from the preset, caps clamped with warnings (prepended to `result.warnings`), structural errors → 422. `vg_curve_stochastic` keeps the stochastic package's own vg_min/vg_max/n defaults.
- [backend-core] Extra internal kind `folds` (`deterministic.run_folds`, folds only) backs the `/api/folds` alias; it is listed in `/api/meta` as `extra_kinds`, not in `kinds`.
- [backend-core] `charge_balance`: `S.state` raises when the r bracket [0, vd−u] ends where `components()` is NaN (depletion edge past the neutral-length limit, e.g. V_D = 5 V, u ≥ 0.91 V); `deterministic.state_row` then shrinks the bracket to the finite part with identical row formulas. The u grid adds 40 log-spaced points on [1e-9, 0.02] V because the low-V_D HRS root sits at u ≈ 1e-7 V.
- [backend-core] `double_sweep` inserts each fold jump as two points at the same V_D (fold current, then the other branch); ≤ 2001 grid points per direction.
- [backend-core] Latch-window edges = latch-existence limits of `classify()` (grid 601) by bisection to 1 mV: −3.906 / −0.810 V vs the paper's −3.90 / −0.815 V (within 10 mV).
- [backend-core] Validation "carrier noise only" breakdown is derived, not copied: compound FPT lattice with one channel class as jumps (II clusters | unit generation GIDL+BTBT_j | REC bulk+junction SRH | DIFF) and the rest as deterministic drift, upwind on a lattice refined N = 4, 8 + Richardson in log h → II 5.1, BTBT 3.0, REC 4.2, DIFF 1.9, all 8.0 mV (paper 4.6/2.7/4.3/1.8/7.8); tolerance ±1 mV. The FPT-node SD check uses VALIDATION.md's ≈ 8 mV (stl_api.py's comment says 6.8 mV; the engine gives 8.03 mV).
- [backend-core] Default worker count is container-aware (affinity + cgroup cpu.max); Docker image runs as uid 1000 with /app writable (numba caches, photo_nodes, server/.cache) and `scripts/warmup.py` at build time.
- [orchestrator] Presets now enable `ld_carrier_noise` by default: without the latch-down FPT the general engine places V_LD exactly at the fold (2.598 V for the paper device vs 2.700 V measured); with it 2.6996 V.
- [orchestrator] Engine finding (physics-A): `photo_mean.components` tests `bulk = p[24] > 0.5`, so p[24] = 2 ("edge excl. channel") is unreachable and equals p[24] = 1. Engine kept verbatim; UI offers 0/1 only.
- [frontend] State holds parameter values exactly as the payload schema defines them (SI or the unit in the key name, e.g. `iph_pA`, `l_gidl_nm`); fields convert only for display via a `scale` (τ in µs/ns, σ_φ and δφ in mV, N_A,acc in 10¹⁷ cm⁻³). Every request sends the full `device`/`sweep`/`stochastic` blocks with `device.preset` = the preset the values were loaded from (paper/photo/custom), so server-side preset resolution is a no-op; "custom (modified from …)" is a UI label computed by comparing with the preset defaults.
- [frontend] Backend selection: `?mock=1` forces the in-browser demo backend; otherwise `/api/health` is probed before any job and a failed probe switches to the demo backend with an "offline — demo data" banner (health is re-polled every 20 s and the UI reconnects). Demo results are badged "demo" per panel.
- [frontend] Log current axes get an explicit initial range (≤ 14 decades below the maximum, floor 1e-17 A) because the engine returns exponentially small currents near V_D = 0 (≈1e-58 A); data are not altered (hover/CSV keep the raw values). The charge-balance quasi-potential U is framed around the roots for the same reason.
- [frontend] Server warnings are rendered as a small collapsible "notices" element under each panel (most runs carry informational warnings), errors as an inline alert in the panel with the server message.
- [frontend] `ext.loc_carriers = 2` is shown but disabled with the note "currently identical to 1 in the engine (photo_mean.py p[24] > 0.5 check)".
- [frontend] Circuit bench parameter keys used by the UI (until `server/compute/circuit/benches.py` documents them): load_line {v_max_V, rate_V_per_s, r_series_ohm, c_node_F}; pulse {amplitude_V, base_V, width_s, period_s, n_pulses, rise_s, r_series_ohm, c_node_F}; pbit {v_bias_V, r_load_ohm, v_threshold_V, duration_s, c_node_F}; coupled {v_max_V, rate_V_per_s, r_series_ohm, r_couple_ohm, vg2_V, c_node_F}. Unknown keys should be ignored and missing ones defaulted server-side. The UI renders any CircuitResult generically (signals grouped by `axis`, summary cards, distributions, sweeps, events, schematic from the netlist) and fetches `branches` for the same device for the I–V trajectory overlay.
- [frontend] Validation figure "photo device 8 conditions" runs `sweep_mc` per measured condition with the photo preset (V_G and optical power replaced, power mode) sequentially on demand; "V_G dependence" runs `vg_curve_stochastic` for the paper preset over −3.6…−0.9 V (10 points) on demand.
- [physics-B] circuit-element topic is aligned with server/compute/circuit/{element,mna,sim,stochastic,benches}.py as of 13:12 (E2 modes BE/TRAP/tau-leap/drift-only, FD Jacobian, draw_dq, solver and bench defaults); docs/CIRCUIT_SIMULATOR.md did not exist yet, so the remaining details carry a notes entry "implementation detail: see docs/CIRCUIT_SIMULATOR.md".
- [orchestrator] Paper-record validation uses the ten records of gate_dynamic_compare.main() (seeds 2026093000–09, pooled) → σ_LU 125.8 mV, σ_LD 19.6 mV exactly.
- [frontend] Supersedes the earlier guessed circuit keys: the Circuit tab now uses exactly `server/compute/circuit/benches.py` BENCH_DEFAULTS / SOLVER_DEFAULTS / STOCHASTIC_DEFAULTS (e.g. load_line {v_min_V, v_max_V, rate_V_per_s, n_cycles, R_s_ohm, C_d_F, vg_V}, pulse {v_base_V, v_amp_V, width_s, period_s, rise_s, fall_s, n_pulses, delay_s, R_s_ohm, C_d_F, vg_V, amplitudes_V[]}, pbit {v_low_V, v_high_V, clock_period_s, clock_width_s, rise_s, fall_s, n_clocks, R_L_ohm, C_d_F, cmp_threshold_V, vg_V, vg_list_V[], light_list_pA[]}, coupled {source, ramp/pulse keys, R_s1_ohm, R_s2_ohm, R_c_ohm, C_d_F, vg_V, vg2_V, iph2_pA}). Server "auto" (None) values are sent as JSON null and shown as an "auto" chip; list parameters are comma-separated inputs.
- [circuit] STL element unknowns (u, r) with E1: V_D(u,r) = v_D - v_S and E2: charge equation on Q = C_ox(psi - V_GS) + Q_exc + q N_A A L_n (identical to `S.state` columns 5+6+7 minus C_ox V_GS, checked to 1e-12). Gate is DC (driven by a V source); the gate/body displacement currents are not stamped (terminal current = the model's quasi-static I_D).
- [circuit] Outside the engine's domain: r < 0 (drain junction forward biased, fast down-ramps / photovoltaic body at V_D ~ 0) -> core at r = 0 plus a symmetric forward drain diode (source-emitter diffusion saturation current without phi_E + depletion SRH); u < 0 (components returns NaN) -> core at u = 0, currents continued as X(0) + X'(0+) V_T expm1(u/V_T), exact psi(u), w_s(u). Both flagged in result warnings with min u / min r and time spent.
- [circuit] Stochastic mode integrates each cell in one of four tiers: (1) event-level explicit tau-leap (Eq. 2, Poisson/compound-Poisson counts, Gaussian limit > 100) with h <= tau_frac*tau_rel (0.05), <= 200 expected events, <= 10 mV drift; (2) Gaussian drift-implicit, variance-corrected (eta ~ N(0, D h (1 + h/2tau))) for 2 ns <= tau_rel with h <= tau_rel/2; also outside the bistable window [V_LD-0.25, V_LU+0.25] V (monostable) with h <= 20 tau_rel; (3) drift only below 2 ns; (4) a latched cell (after latch-up, before latch-down) is drift-only unless stochastic.ld_carrier_noise = true (same key and meaning as §1: resolving the LRS noise costs ~15 s per 1200 V/s cycle and moves V_LD by ~+50 mV for the photo condition).
- [circuit] Latch-down is detected at I_D < i_threshold/hysteresis (detect.hysteresis, default 10 -> 1 nA); latch-up at I_D >= i_threshold (10 nA). The LRS current at the LD fold is only ~15 nA, so a single threshold chatters under carrier noise.
- [circuit] local_state "frozen" = one draw per run (whole waveform, all cycles); "evolving" = OU in time, continuous across cycles. acquisition_trend is ignored (warning).
- [circuit] load_line/coupled rate default = preset sweep rate; in stochastic mode with carrier noise, when the estimate exceeds max_steps/2 and the user did not set the rate, 1200 V/s is used (warning). Runs whose estimate exceeds 2x max_steps (per run) or 4e7 steps (per request) are refused with ValueError.
- [circuit] Result carries extra keys besides §4: bench_params (resolved), solver, detect, stochastic, folds {V_LU, V_LD}, feasibility, regimes; events may carry cell / cycle / pulse / clock fields and the extra kind "pulse" (value 1 = switched). server/compute/circuit/__init__.py imports no numba (heavy modules load inside run_circuit).
- [circuit] (supersedes the "monostable Gaussian h <= 20 tau" part of the tier bullet above) Carrier noise is resolved only inside per-cell noise bands, where the HRS/LRS barrier to the saddle is < noise_z_max = 12 stationary SDs (calibrated against the exact compound hazard: z = 12 <-> <~1e-3 /s); outside the bands the cell is drift-only (large Gaussian steps inside a bistable region could land Newton on the other basin). This makes the paper device at 0.4 V/s event-level feasible (~2.3e5 steps, ~5 s per cycle), so the paper preset keeps its 0.4 V/s rate in stochastic mode.
- [circuit] Stored stochastic runs: run 0 is decimated to <= 4000 points, runs 1..7 to <= 1500 points (time + arc-length sampler), keeping a result at a few MB; `trajectory` is run 0. Stats "spread" = SD over runs (P_sw, P1: SD of the per-run fractions).
- [physics-fix] Physics topics quote fold states refined like the app's deterministic `folds` readout (vertex u of the 3-row parabola, I_D log-interpolated in u; grid-row values labelled as such), name the lag-1 estimator (app Stats.lag1 = Pearson of consecutive pairs, stoch_core; measured_stats.json/Data tab = autocovariance ratio), and document the circuit's `stochastic.ld_carrier_noise` default as false (benches.STOCHASTIC_DEFAULTS, tier 4) while both device presets use true.
- [frontend-fix] Sticky header and mode strip are fully opaque (the strip is the soft accent tint layered over `--surface`), and every `.plot` is `isolation: isolate` so Plotly's modebar/tooltip z-indices (1001+) stay inside the plot; plots and legends now scroll underneath.
- [frontend-fix] Display equations (`FitTex`): shrink to fit their box down to 80 % font size; if still too wide they scroll horizontally inside their own box (thin visible scrollbar, right-edge fade, keyboard-focusable region). Never clipped; re-fitted on Details-window resize and after KaTeX fonts load.
- [frontend-fix] The mode strip / toggle hint is tab-aware: Circuit tab describes the transient (deterministic: STL state-space element in MNA, implicit BE/TRAP (current solver choice) + Newton, adaptive Δt; stochastic: Eq. 2 event increments of Q_B + local states, statistics over runs); Validation/Physics say the mode applies to the Device and Circuit tabs.
- [frontend-fix] Circuit `bench_params` keys left on auto (`null`) are omitted from the request so `server/compute/circuit/benches.py` BENCH_DEFAULTS apply (the server's merge would copy an explicit null over a numeric default). `rise_s`/`fall_s` of pulse, p-bit and coupled now default to auto (server 10 µs / 20 µs; the old 1 µs edges kick the floating body and latch below the fold); persisted v1 values equal to the old 1 µs default migrate to auto (schema version `v` = 2 in the same localStorage key).
- [frontend-fix] Persisted UI state is validated field by field (`web/src/state/persist.ts`): enums (tab, mode, lang, theme, preset, light mode, local-state mode/action, engine, bench, solver method, coupled source, loc_carriers), numeric lists and V_G ranges; junk/`null`/older data falls back to defaults instead of a blank page.
- [frontend-fix] The Run bar reports a finished run only in its own context (device + mode, or circuit + mode); running jobs are always shown (cancellable). Run groups finish by identity, so an overlapping re-run of the same panels is not marked done by the older one.
- [frontend-fix] "Parameters changed" is shown consistently: current-components, V_LU/V_LD distribution and cycle-series panels, the circuit distribution/sweep/events panels, and the KPI strips (values dimmed + badge).
- [frontend-fix] Circuit latch-detect threshold (`detect.i_threshold_A`) moved from the stochastic-only group to the Solver group (it is used by both modes). Empty result panels carry their own Run button (the sidebar Run button is inside the drawer below 1100 px); the drawer closes when the window shrinks across the breakpoint. Components panel falls back to the full locus when there is no latch (HRS/LRS empty).
- [frontend-fix] `l_GIDL` (p[4]) is described as the effective GIDL field length, E_G = max(u + r − V_G − 0.3 − 1.12 + φ_GIDL, 0)/l_GIDL (photo_mean.py), not as the BTBT region length (the GIDL volume uses a fixed 5 nm).
- [api-fix] Jobs are split into client handles (`Job`, own id/status) and computations (`_Run`): identical in-flight payloads share one run but get distinct job ids, so one client's DELETE no longer cancels another client's identical job; the run is cancelled when its last handle is cancelled. Handles nobody polled for `STL_ABANDON_S` (600 s) are cancelled (closed tabs).
- [api-fix] Admission control: at most `STL_MAX_PENDING` (64) queued/running handles in total and `STL_MAX_PENDING_PER_CLIENT` (16) per client address → HTTP 429 + Retry-After (the queue used to be unbounded, and finished jobs were evicted the moment they finished once > 200 were pending). `GET /api/jobs` lists only the caller's jobs (the global list exposed every job id, i.e. let anyone cancel anyone's jobs). Client identity = `request.client.host` (X-Forwarded-For under the Docker `--proxy-headers`; spoofable without a proxy, the global cap still holds).
- [api-fix] Long-polls (`POST /api/compute?wait`, `GET /api/jobs/{id}?wait`) wait on the event loop instead of holding one of Starlette's 40 threads (45 concurrent long-polls stalled /api/health for 19 s). Request bodies are capped at `STL_MAX_BODY_KB` (256 KiB, 413) by an ASGI middleware that also covers chunked uploads; `payloads.check_tree` rejects depth > 12, > 5000 values, strings > 1000 chars, NaN/Infinity literals and ints beyond float range, and nested blocks must be objects — these were HTTP 500s (OverflowError, RecursionError in deepcopy, AttributeError/TypeError). All errors are `{"detail": "<string>"}` (FastAPI's list form echoed the input back); unexpected ones are a JSON 500 without internals.
- [api-fix] Worker crash (SIGKILL/segfault/OOM): the pool is restarted immediately (not at the next submit) and the runs that were in it are retried; only a run that had started counts an attempt, so the crashing run fails after its second crash while queued innocents keep being retried. Workers and the Manager process run a parent-death watchdog (`mp.parent_process().join()` → `os._exit`), so a SIGKILL/OOM kill of the API no longer leaves ~200 MB orphans re-parented to init.
- [api-fix] Caches: the disk result cache is kept below `STL_DISK_CACHE_MB` while running (was pruned only at start-up), reads refresh mtime (LRU); the stochastic node cache `server/.cache/stochastic` (unbounded before) is pruned to `STL_NODE_CACHE_MB` at start and every 10 min; finished jobs keep at most `STL_JOB_RESULTS_MB` (64) of result bytes, older results are re-read from the cache. ENGINE_VERSION now also hashes `engine/**` code + data (run-time cache folders excluded), `server/jsonutil.py` and `server/jobs.py`. `jsonutil.canonical` keeps integers a float cannot hold exactly (seeds > 2**53 used to collide; > 64-bit ones crashed orjson); `stochastic.seed` is no longer rounded through float.
- [api-fix] Dockerfile: requirements are copied to /tmp (copying them to /app/server first created /app/server as root and `COPY --chown` keeps an existing directory's owner, so uid 1000 could not create server/.cache); `scripts/warmup.py` also runs a 1-cycle deterministic + stochastic load-line circuit so the circuit numba kernels (~40 s cold compile) are cached in the image.
- [frontend-fix] HTTP 429 (job queue full, `Retry-After`): the client waits Retry-After (bounded 0.2–60 s, default 5 s), shows "서버가 바쁩니다 — N초 후 다시 시도합니다 / Server busy — retrying in N s" in the panel and Run bar, and retries the submit exactly once (cancellable while waiting); a second 429 becomes a localized "queue full, run again" error. Error bodies are read from `{"detail": string}`.
- [engines-fix] Fold parabola across a gap: `deterministic.classify_checked` rejects a MODEL.classify fold whose three rows are not contiguous in u (step > 0.05 V; real folds are one grid step apart, ≤ 5.5 mV at grid 201, the gaps seen for V_G ≳ 0 V (paper) / ≳ −0.2 V (photo) are ≥ 0.83 V) → latch = false + "locus not traceable" warning. Used by every kind (branches, folds, vg_curve, latch-window bisection, stochastic fold/hazard nodes via stoch_core.classify). Only the fold rows are checked: latching V_G down to −0.9 V have legitimate low-V_D gaps in the HRS (u 1e-17 → 0.07…0.4 V), so a whole-locus check would move the −0.810 V window edge. For a rejected fold the double sweep follows the locus only up to its first gap.
- [engines-fix] calibrated_lookup requires ΔV = 10 mV/k (`stochastic.calibrated_dv_ok`: round(4/ΔV) == 400·round(0.01/ΔV)); `auto` falls back to `general` with a warning, an explicit request raises ValueError. Traces use the engine's own read-out grid (linspace(0,4,steps+1)[::k]).
- [engines-fix] General MC: the hazard integral and fold crossing run on sub-steps of ≤ 2 mV (`stoch_mc.HAZARD_SUBSTEP_V`), ΔV only sets the state sampling (sample-and-hold within a step). ΔV ≤ 2 mV is bit-identical to before (photo validation unchanged); paper none-mode ΔV 0.5/2/10/50 mV now all give V_LU 3.6444–3.6445 V / 7.97 mV, V_LD 2.6994–2.6995 V / 6.64 mV (before: 3.6100 V / 15.4 mV σ_LD at 50 mV).
- [engines-fix] vg_curve_stochastic censors at sweep.vd_max_V like sweep_mc: V = F (81-point fold mixture) + ε (GH-node quantile offsets pooled and recentred on the common mean shift) conditioned on V ≤ vd_max (`stochastic.truncated_mixture`, law-of-total-variance split into state_sd/noise_sd). Identical to the previous moment formula when nothing is censored (the 0–6 V validation check is unchanged). New result keys: beyond_sweep_weight, censored_weight (= no_latch + beyond), vd_max_V. The slow test for the 4.354 V point now uses the 0–6 V sweep (as validation.check_vg_curve_stochastic); with the preset's 4 V sweep that point is > 50 % censored.
- [engines-fix] Hazard curves: rows via `deterministic.state_row` (= S.state where it succeeds). The real limit for folds above ≈ 5.1 V (e.g. l_GIDL ≳ 42 nm) is the avalanche cluster kernel range r ∈ [0.7, 5.0] V (compound_fpt.backward asserts): such voltages are now counted (`kernel_skipped`, `kernel_r_V`, `n_voltages` in the record) and warned about explicitly instead of "window too narrow"; escapes there stay at the fold (no kernel extrapolation).
- [engines-fix] stoch_core.fold_node returns the refined fold states of `deterministic.folds_of` (vertex u, log-interpolated I) — identical to the `folds` readout; CACHE_VERSION → stoch-3 (old node caches are ignored, rebuilt on demand).
- [engines-fix] MC CDFs are normalised to all cycles (`stoch_core.ecdf(v, n_total)`): LU plateaus at the latched fraction; LD population = latched-up cycles (general) or all cycles (calibrated, unpaired records), latch-downs not reached by 0 V enter as an offset. cdf blocks carry n/n_total. The frontend's own ecdf (StoPanels.tsx) is separate.
- [engines-fix] Deterministic double sweep: coarsened to ≤ 2001 points per direction silently (display resampling of the traced branches; fold jumps inserted exactly), step reported as `sweep_dv_V`; the refined fold points (V_LU, I_LU) / (V_LD, I_LD) close the HRS/LRS interpolants, so no null current next to the jumps.
- [circuit-fix] Latch state = the body's branch, not a current threshold: along MODEL.classify's branch (parameterised by u) HRS u < u_i (LU-fold u), LRS u > u_j (LD-fold u); a cell latches at u >= u_j and unlatches at u <= u_i (no window -> never). This state drives the noise tier/band, P_sw, P_retained and P_latched. detect.i_threshold_A / hysteresis only *time* the events (crossing inside the switching transient, else the body crossing); for the default thresholds in the STL regime the reported V_LU/V_LD are unchanged. I_D threshold crossings with the body on the HRS (channel conduction above V_G ~ -0.5 V) are counted and warned, never reported as latch-up. Out-of-range thresholds are warned (not clamped).
- [circuit-fix] The circuit takes its branches/folds/u_i,u_j from `deterministic.classify_checked` (lazy import; stochastic.classify_checked wrapper) in stochastic.py, runner.py and validate.py, so a fold fitted across a locus gap (V_G = +1 V: rows u = 0 -> 0.885, vertex 9.3 V) is "no latch" in both tabs; the circuit then reports no latch-up and warns.
- [circuit-fix] Noise bands have no edge beyond the fold (unlatched: [V(z=12), inf), latched: (-inf, V(z=12)]), and the kernel resolves the noise 4 tau_rel before the drive (main PWL source) enters a band (look-ahead, time-based, so light-biased pulse trains with tau ~ 5 us everywhere do not pay for noise at v_base). Fast ramps now equal the no-band result exactly (3e4 V/s: 4.2594 V / 13.1 mV vs old 4.2508 / 5.4 mV); supra-fold pulse delay SD 0.1 -> 1.2 us. Devices without a latch window are drift-only unless local states are on.
- [circuit-fix] Default BE LTE_u tightened from 1 mV to 30 uV (reltol 1e-3): ramp lag within ~1 % of converged (1200 V/s 44.0 vs 44.4 mV; 40 V/s 5.00 vs 5.07 mV) for +20 % steps (load line) / +50 % (pulse train); TRAP was not made the default (still 5 % off at 40 V/s).
- [circuit-fix] Truncated runs: unreached pulses/clocks/cycles are censored (NaN, excluded from P_sw, P_retained, P1, lag1, sweep points, n_latch_up denominator = observed cycles) with a count in the warnings. Regime times are cell-averaged; latched steps count as tier 4. Stochastic mode without any randomness runs once (warning). local_avalanche with ext.aloc = 0 substitutes aloc = 1.0 like the device MC (StateMap). pbit `P1` is labelled "comparator output" and `P_latched` is added.
- [circuit-fix] Feasibility estimate rewritten (voltage-resolved grid, look-ahead, empirical compound-noise escape hazard lambda(z) = 3.3/s * 10^(-1.2 (z - 7.5)), 60 steps per waveform corner): load lines 0.80-1.08 of actual, stochastic probabilistic pulse/p-bit trains up to 1.6x (conservative). Deterministic refusals no longer mention carrier noise.
- [physics-lang] Physics topics (web/src/content/physics/topics/*.ts) edited for language only; tex, equation ids, code refs, variable symbols/units/codes and every number are unchanged (checked by extracting all `tex:` strings, ids and variable rows before/after; equation counts identical per topic). Register: Korean prose in 한다체 (textbook style; the UI dictionary keeps 합니다체 for UI sentences), labels/headings/variable names/list items as noun phrases without a final period; English in sentence case with American spelling (matches `i18n/strings.ts`), no serial comma. Device wording: "FDSOI · L_g 500 nm · W 200 nm · T_Si 50 nm · EOT 14.1 nm" (roadmap PDSOI/Bulk mentioned once in overview); "paper device/parameters/model" → reference calibration (기준 보정) / illumination calibration (광조사 보정) / base model = all extensions zero (기본 모델); measured data → reference record (기준 측정 기록) / illumination records (광조사 측정 기록); "handoff" → "not included in engine/" (엔진 코드(engine/)에 없음). Remaining "paper" hits are code identifiers only (`check_paper_records`, the reader_fig3 path). Glossary (ko ↔ en): 바디 ↔ body (UI still writes "body"), 드레인/소스/게이트, 이미터/베이스/컬렉터, 래치·래치업·래치다운 ↔ latch/latch-up/latch-down, fold and hazard kept in Latin script (hazard glossed once as 단위 시간당 탈출률), 첫 통과 ↔ first passage, 국소 상태 ↔ local state, 드레인/소스 가장자리 ↔ drain/source edge, 캐리어 잡음 ↔ carrier noise, 충돌 이온화 ↔ impact ionization, 애벌랜치 ↔ avalanche, 시드 전류 ↔ seed current, 가지 ↔ branch, 우물/안장점 ↔ well/saddle, 중도절단 ↔ censored, fold 점질량 ↔ fold atom, 드리프트 ↔ drift, 가우스 ↔ Gaussian, 후향 방정식 ↔ backward equation, 고정/진화/없음 ↔ frozen/evolving/none, 획득 추세 ↔ acquisition trend, 난수 시드 ↔ seed (RNG), 계층 ↔ tier, 잡음 대역 ↔ noise band, 선행 감지 ↔ look-ahead, 부하선 ↔ load line, 접근 저항 ↔ access resistance, 내장 전위 ↔ built-in potential, 준페르미 준위 ↔ quasi-Fermi level.
- [brand] Logo: stylised biristor-style two-terminal symbol (terminal leads + rounded body) whose body holds the S-shaped latch characteristic (HRS branch → fold → faded unstable branch → fold → LRS branch), white on a rounded-square badge with a teal → indigo → violet gradient (deterministic → stochastic accents; tokens --logo-a/b/c, slightly brighter in dark theme) plus a soft top-left highlight and a 1.4 px symbol shadow. Chosen from 7 variants (hysteresis-step, Schmitt-loop-in-box, free S-curve with terminal dots, …) because it stays readable as "two-terminal device" at 16 px. One geometry in web/public/favicon.svg (fixed colours) and web/src/components/Logo.tsx (CSS-token colours, useId gradient ids). It is an interpretation — replace the glyph if the lab supplies the exact biristor symbol.
- [brand] Credits live at the right end of the sticky mode strip (never over plots or the Run bar): full text ≥ 1440 px, "KAIST · NOBEL 연구실 (최양규 교수) · 개발 박준형" 1101–1439 px, "KAIST · NOBEL 연구실" ≤ 1100 px, icon only ≤ 760 px. Click → non-modal About card (portal, Esc/outside click/× close, focus returns to the chip) with description, device + "unpublished model — for research use", lab / advisor / institution / developer and app/engine version from /api/health (app_version, engine_version[:8]). No e-mail, no URLs, NOBEL not expanded. Mounted from Header.tsx's ModeBar (App.tsx untouched).
- [brand] Header: Logo 34 px + "STL Simulator" + technology chip "FDSOI" (tooltip with L_g/W/T_Si/EOT) + subtitle "SOI 단일 트랜지스터 래치 시뮬레이터" / "Single-transistor latch simulator for SOI" (the "deterministic & stochastic" part is dropped: the toggle right next to it says so, and the longer subtitle squeezed the header at 1366–1440 px). Tab/mode English captions now show only ≥ 1440 px; ≤ 1100 px the chip hides and header gaps shrink (no squeeze at 1024).
- [brand] Wording: the model is unpublished, so no user-facing "paper/논문/Fig. 3(b)". Preset ids stay (paper/photo/custom); short names 기준 보정 / 광조사 보정 / 사용자 정의 (Reference / Illumination / Custom); full labels (server/params.py PRESETS + web/src/state/presets.ts fallback) "FDSOI · L_g 500 nm · W 200 nm · T_Si 50 nm · EOT 14.1 nm — 기준 보정 (암조건, V_G = −2 V, 0.4 V/s)" etc. Each preset and /api/meta now carry technology "FDSOI" and geometry {Lg_nm, W_nm, Tsi_nm, EOT_nm} (+ meta.technologies with PDSOI/Bulk available=false); presetTech(meta, id) in presets.ts reads them with a built-in fallback (no api/types.ts change). build_p/presets numerically identical.
- [brand] Glossary (with the orchestrator's KO decisions): KO labels = noun phrases, sentences = 합니다체; EN = sentence case, American spelling (center, ionization, color). latch-up/latch-down, branch, fold, hazard stay in Latin script in KO; 바디 (body), 국소 상태 (local state), 캐리어 잡음 (carrier noise, also for the circuit "event-increment noise"), 첫 통과 (first passage), 중도절단 (censored), 기준 보정 / 광조사 보정, 기준 측정 기록 (reference record), 기본 모델 (base model = all extensions zero; replaces "paper model"), 래치 창 (latch window = V_G range with a pair of folds) vs 히스테리시스 창 (hysteresis window ΔV). Korean text breaks between words (`word-break: keep-all` under :root[lang=ko]). Sidebar labels shortened so none truncates at 1280/1440 px in KO/EN (full meaning moved into the ⓘ help), except the circuit bench's "Drain-node capacitance" (params/benches.ts, circuit package).
- [brand] Server-side user-facing strings without "paper": validation.py check labels/expected/notes (ids unchanged), stochastic.py measured-record labels and three warnings ("reference record", "illumination record", "reference calibration"). Not changed (other owners): server/compute/circuit/stochastic.py warning "calibrated paper-record lookup engine"; KpiStrip.tsx hard-codes KO "래치업/래치다운" for the stochastic KPI labels (glossary says latch-up/latch-down).
- [stats] `web/src/stats/describe.ts` mirrors numpy/scipy/stoch_core exactly: numpy "linear" quantiles (same virtual-index rounding), sd ddof = 1, scipy `skew/kurtosis(bias=False)` (G1, excess G2), lag1 = stoch_core.lag1 (Pearson over consecutive pairs of the *raw* series with both finite — pairs across a censored cycle are dropped, ≥ 3 pairs, constant-series guard 1e-9 V; the guard is relative below |x| = 1 so µs/nA quantities are not treated as constants). Verified identical (≤ 1e-12 relative) to the server's Stats on the photo and reference presets (fixtures in `web/src/stats/fixtures/`). Extra field `ci95_half` (t(0.975, n−1)·SD/√n; Student t via regularized incomplete beta, checked against scipy.stats.t.ppf).
- [stats] `ks2` p-value = classic asymptotic Kolmogorov Q_KS(√(nm/(n+m))·D) (= scipy.stats.kstwobign.sf), not scipy's newer `ks_2samp(method="asymp")` (kstwo with rounded n_e, ~0.02–0.04 lower p at n = 100–400). Tooltip warns that cycle-to-cycle correlation makes p too small. Censored (null) values are excluded from KS and moments, counted in n_total/censored.
- [stats] `histogram` = numpy.histogram semantics with Freedman–Diaconis default (numpy "fd"; bins clamped 1…100, overridable); a sample without spread gets one bin ±0.1 % around the value (numpy: ±0.5) — the server's `stoch_core.histogram` additionally clamps FD to 10…80 bins. `ecdf(values, n_total)` = stoch_core.ecdf (normalised to all cycles).
- [stats] Device tab, Stochastic: the full-width "통계 요약 / Statistics" panel is rendered by `KpiStrip` right under the KPI strip (DeviceTab.tsx untouched). Rows V_LU, V_LD, per-cycle window V_LU − V_LD (null where either transition is missing), and a secondary analytic row "V_LU, carrier noise only" from the hazard result (server Stats + q1/q3 interpolated from `quantiles`; shown only when the hazard result belongs to the same parameter set as the MC result). Measured records appear as a sub-row under each quantity (measured layout "rows"); the comparison columns (Δmean, SD ratio, KS D, p) sit right after centre/spread. Meta chips: engine, cycles, seed, sweep, censored count/share, 95 % CI of mean V_LU.
- [stats] StatsTable units: one plan per table when all rows share a unit, else per row (unit chip under the label). Voltages: levels in V with ≥ mV resolution (decimals follow the row's SD: ≈ a tenth of the spread), spreads in mV; other SI units pick prefixes from magnitudes. Low-priority columns (min, max by default, `optional` prop) are hidden automatically when the table would overflow its card, with a toggle; they are always in the CSV. CSV = every statistic, full precision, row unit, model + measured rows.
- [stats] The old DistPanel stats table was replaced by the statistics panel (the `stats-table` test id now belongs to it). DistPanel's CDF uses the result's `cdf` block (normalised to all cycles; fallback `ecdf(values)`), measured CDF via `ecdf`; KS D/p and the "CDF levels off at the switched share" note are in the panel footer (in-plot annotations collided with curves). The measured caption is built from the run (cycles, V_G, light) instead of the server's `measured.label` (which still says "paper device" — server/compute/stochastic.py should rename it).
- [stats] V_G curve (stochastic): `no_latch_weight` + `beyond_sweep_weight` drawn as stacked % bars on a right axis of the σ strip, only when some in-window V_G value is > 0.1 % censored; footer states the peak censored share and that mean/σ are over the cycles that latch within the sweep. Types: optional `beyond_sweep_weight`, `censored_weight`, `vd_max_V` (V_G curve), `n_voltages`, `kernel_skipped`, `skipped`, `step_V` (hazard), `sweep_dv_V`, `vd_max_V`, `grid` (branches), `n`/`n_total` (Cdf).
- [stats] Validation, 8 illumination conditions: a StatsTable (measured layout "columns": mean, SD, measured mean/SD, Δmean, SD ratio, KS D, p) under the figure (`web/src/validation/PhotoStats.tsx`; ValidationTab.tsx only gained the import and the child element). It fits the grid cell without scrolling.
- [circuit-custom] `bench: "custom"` is dispatched from `runner.run_circuit` to the new `server/compute/circuit/custom.py`; it drives the existing MNA kernel (`sim.simulate`) — no separate simulator. Kernel additions are all inactive for the benches (bench results bit-identical to before: 5 deterministic/stochastic/local-state/coupled cases compared, same events and step counts): per-cell noise look-ahead drive and per-cell local-state configuration as extra columns of the per-cell `win` array (N_WIN 6 → 15, no signature change), capacitor currents appended to the record rows, optional node-voltage LTE (`CF_LTEV`, SPICE-like 7·(reltol|v| + 1 µV), custom only), `ns = 0` guards.
- [circuit-custom] ERC follows SPICE practice: every node needs a DC path to ground through R, V or an STL drain–source path (C, I sources and STL gates do not conduct DC) → capacitor-only islands and undriven gates are errors; single-connection nodes, shorted R/C/I and STL terminals on one node are warnings.
- [circuit-custom] The STL gate voltage comes from the circuit (the device block's `vg` is ignored, warned when it differs). Latch-state fold values u_i/u_j, noise bands and the fold summary use the quasi-static branches at the V_GS and light the cell sees (linear DC estimate with the cells open); for time-varying V_GS or light at both extremes (u_i min, u_j max over variants with a latch window, band union).
- [circuit-custom] Noise look-ahead for general circuits: per cell the source whose waveform moves the cell's open-circuit V_DS most, V_DS(t') ≈ V_DS(t) + g (w(t') − w(t)) with g from the linear DC estimate (benches keep V_DS ≈ w). The custom load line reproduces the stochastic bench event for event.
- [circuit-custom] Per-STL `local_state` (library setting, sent by the frontend) is honoured in stochastic mode; `stochastic.local_state_override: true` applies the global block to all cells (WEB_CONTRACT §6.3).
- [circuit-custom] Server-generated waves (pulse corners, sine samples at 128 points/period) may have up to 20 000 points (user PWL ≤ 2000 as in §6); zero rise/fall and vertical PWL steps become finite edges min(dt_max, 1e-3 t_stop, 0.1 pw, 0.1 per) (warned).
- [circuit-custom] Output: every accepted step is recorded and reduced on the fly (arc length over the signals + uniform time + forced source corners and latch events); run 0 ≤ 4000 points and ≤ 400 000 values, runs 1–7 ≤ 1500 points / 100 000 values, envelopes ≤ 1000 points; plotted values rounded to 7 significant digits (t 10, envelopes 6) for page weight (1-STL deterministic result 0.45 MB JSON / 0.13 MB gzip).
- [circuit-custom] Summary/distribution keys follow the frontend mock (`X1.t_first_lu`, `X1.vd_first_lu`, `X1.p_any_lu`, `X1.p_latched_end`, `X1.final_state` "LRS"/"HRS", `end:<key>`); `op` is a flat {signal key: value} map at t = 0.
- [circuit-custom] Feasibility: Δv/dt_max steps of every node of the linear network (clipped to ±max(10 V, 1.5 max|V|)) with corner sharpness weights + the bench estimator per cell along its open-circuit drive; cells add only their own extra steps. Current-biased cells are warned (possible relaxation oscillation, not predictable by the estimate; bounded by max_steps).
- [circuit-custom] `server/payloads.py`: bodies with `bench: "custom"` may hold 40 000 JSON values (PWL waves), `netlist`/`tran` must be objects.
- [schematic] Circuit tab has two sub-views: **Schematic editor** (default, lazy chunk `web/src/schematic/**`) and **Quick benches** (the previous bench UI, unchanged). The choice persists in `localStorage["stl-websim:circuit-view"]`; while the editor is active the circuit-tab Run action (Run button, Ctrl+Enter, empty-panel buttons) runs the drawn circuit through a small hook in `state/runner.ts` (`setCircuitRunOverride`). Existing e2e tests that use the benches now click `circuit-view-benches` first.
- [schematic] Custom lightweight SVG editor (no diagram library): 10-px grid, two-terminal parts 80 px pin-to-pin, STL = 3-terminal MOSFET-style symbol (D up, G left, S down) with a small hysteresis mark; nets by union-find over pins, wire end points, T-junctions on wire interiors and net labels (same text = same net, "0"/"gnd" = ground); unnamed nets N001… ordered top-left first. Moving parts drags attached wire ends and re-routes them as an L.
- [schematic] SPICE number semantics in every value field: case-insensitive scale factor ("M"/"m" = milli, "meg" = mega, bare "F" = femto), trailing unit letters ignored; the interpreted value is shown under the field while typing.
- [schematic] Request per §6/§6.3: node names are the net names; `tran.dt_max_s`/`dt_min_s` are always sent (auto = t_stop/2000 and max(1e-15, 1e-13·t_stop)); `probes` = null when "save all" is on (default), else the chosen traces filtered to keys valid for the netlist (never `[]`). Each STL carries its library `local_state` snapshot; `stochastic.local_state_override` = the "override device local states" switch; `stochastic.local_state` = the override setting, or the first STL's setting. `acquisition_trend` is forced off in circuits (device-record engine only; the server warns otherwise).
- [schematic] STL instances store a snapshot {libId, name, device block, local_state}; the properties panel shows whether it still matches the library entry and offers "update from library". The device block's V_G is shown as not used (V_GS comes from the gate node, as the server does).
- [schematic] Client ERC mirrors the server (§6.3): no DC path to ground / gate-only / current-source-only nodes / V loops / no ground / invalid values and names are errors; dangling single-terminal nodes and unconnected pins are warnings when the node still has a DC path to ground (errors otherwise); shorted R/C are warnings. Server ERC errors are shown in the panel and the named elements/nodes are highlighted on the canvas.
- [schematic] Feasibility hint = heuristic calibrated on the live backend (≈ 50 µs per step per STL deterministic, ≈ 75 µs stochastic; steps ≈ t_stop/Δt_max + 30/source corner + 300/possible latch transition, stochastic + 0.3·t_stop/1 µs per STL). "heavy" (> 60 s) or "over the limit" (> 2·10⁶ steps/run) asks for confirmation before submitting; the server's own feasibility check stays authoritative.
- [schematic] Results: the time cursor starts just after the first latch-up of run 0 (else mid-span); annotations use run 0 or the ensemble mean (`envelopes`) in stochastic mode; stochastic plots show runs 0–7 thinly + mean ± SD band; statistics use the shared stats module (`describe`, `histogram`, `StatsTable`); `end:<key>` distributions are hidden behind a toggle.
- [schematic] While the server answered "unknown bench 'custom'", the editor fell back to a clearly marked demo result (`api/mockCustom.ts`, lazily loaded mini MNA with a hysteretic STL stand-in); the fallback triggers only on that error, so the live backend is used automatically now that it supports bench "custom".
- [schematic] Device library (`web/src/devices/**`): user devices in `localStorage["stl-websim:devices"]` (validated field by field on load/import against the reference device block); built-ins are derived from `/api/meta` presets (display name = the calibration part of the preset label after " — "). The sidebar preset card became a compact Device card (technology selector FDSOI active / PDSOI, Bulk "soon", geometry, calibration presets with the old `preset-*` test ids, saved devices, "Save as device"). CSS classes are `techsel*` (the brand package owns `.tech-chip`).
- [schematic] Plotly switched from `plotly.js-dist-min` to `plotly.js-cartesian-dist-min@3.7.0` (same version; the app only uses scatter, bar, histogram, heatmap and contour): Plotly chunk 4,611 kB → 1,376 kB (gzip 455 kB). The editor chunks total ≈ 115 kB (SchematicView 64 kB).
- [orchestrator] Korean UI uses the transliterations 래치업/래치다운 (as the physics text, circuit and statistics strings do); body → 바디; branch/fold/hazard stay in Latin script. Server-side user-facing labels no longer mention "paper" (custom-circuit STL echo, data descriptions, circuit warnings).
- [oscillator] Owner check "a current-driven STL must oscillate" (I_in → V_out, C_par to ground, STL drain on V_out, DC gate): the sawtooth itself was already right (period within 0.1 % of BE/TRAP at reltol 1e-5, events exact), but with a DC current source the DC operating point is the *unstable equilibrium on the NDR branch* (I_D = I_in, u_i < u < u_j), so runs started there and the first spike came at an arbitrary time set by round-off (63 ms instead of 37 ms at 100 pA / 1 pF), and for I_in ≳ 5 nA the operating point did not converge (error). Fix: `tran.initial` = "auto" (default) | "op" | "zero" for custom circuits. "zero" = discharged capacitors (held at 0 V by 1 MS in the DC solve, V-source-fixed capacitors excepted; SPICE UIC with IC = 0); "auto" = "op" unless a high-impedance cell's load line misses the HRS at t = 0, the OP puts a cell on the NDR branch, or no OP is found → "zero" with a warning. The benches keep "op" (bit-identical).
- [oscillator] Feasibility of high-impedance cells (d–s port resistance > 100 MΩ, capacitance on the drain): a quasi-static hysteretic load-line walk C dV/dt = I_N − G V − I_HRS/LRS(V) (`circuit/oscillator.py`, numba) replaces the open-circuit drive (which was clipped to ±8.5 V and gave 2 256 steps for every oscillator, 0.2–0.3× actual; 1.5e6 and refusals at I_in ≥ 30 nA); 400 steps per latch transition for these cells (calibrated on 18 C/I cases: estimate/actual 0.89–1.67 deterministic, 0.82–1.90 stochastic). The walk's drain/source nodes are excluded from the linear-network part, whose total-variation bound is capped at 2 v_clip per corner (a pulsed current source gave 1e5 instead of 5.8e3). It also yields the regime warning (oscillator / settles on HRS / stays latched) and the echo `oscillator`.
- [oscillator] Step control unchanged: LTE_u (30 µV) binds ~90 % of the ~1 000 steps per cycle, but it is what converges the fold lags, and 10 cycles take 0.4–0.8 s (deterministic) / 0.5–2 s per stochastic run (8 s per run only at 3 V/s ramps). Oscillation metrics are additive summary keys (`X1.period`, `X1.f_osc`, `X1.isi_cv` = mean within-run CV = spike-timing jitter, `X1.period_cv_runs` = run-to-run spread from local states, `X1.vd_lu_mean`, `X1.vd_ld_mean`, distribution `X1.isi`), emitted whenever a run has ≥ 2 latch-ups.
- [oscillator] Schematic example "Current-driven oscillator (integrate-and-fire)": 1 nA, 1 pF, V_G −2 V, 15 ms, dt_max 5 µs (10 spikes at 860 Hz, ~1 s; the stochastic 20-run default ≈ 20 s). The current source is drawn from ground rotated 180° (arrow up into the `out` rail); only `out` is labelled so the default traces are V(out) and I(X1.d). Chosen over 100 pA / 1 pF / 150 ms because the frontend stochastic heuristic (0.3·t_stop at 1 µs) would flag the longer run as "heavy".
- [snapshot] Static snapshot for the published page (no backend reachable): when `/api/health` fails the runner loads `./snapshot/index.json` (document-relative) and switches to `backend: "snapshot"` (`web/src/api/snapshot.ts`); no index (404 or an SPA HTML fallback) → the demo backend as before; `?mock=1` still forces demo mode. Health polling is skipped in snapshot mode (the status dot re-probes).
- [snapshot] Key = SHA-256 (hex) of the canonical JSON of `{kind, payload}` (JSON semantics — undefined dropped, NaN/±inf → null — and recursively sorted keys). SHA-256 is implemented in plain JS (synchronous, works without a secure context, unit-tested against Web Crypto); the recorder script re-derives every key with node:crypto and fails on a mismatch.
- [snapshot] Files: one gzip JSON per result (`<key>.json.gz`, decoded with `DecompressionStream("gzip")`; bytes without the gzip magic are read as plain JSON in case a host already removed the gzip layer); optional plain `.json` copies (`record --plain`, or `build:artifact --plain` rewrites the build to plain JSON if a host refuses `.gz`). Health and meta are embedded in `index.json`; measured data and the design map are files. Grid results are bundled (`grid-<kind>-<n>.json.gz`, `{key: result}`, ≈ 3 MB JSON each) and fetched once per bundle.
- [snapshot] Misses (request not in the snapshot) are answered by the demo backend; the result object is registered in a WeakSet (`isSnapshotFallback`) so `runKey`/`runSchematic` mark the entry `mock`, the Panel shows "not in the static snapshot — example data" and the banner adds a line. Snapshot hits are returned as `status: done, cached: true` without polling.
- [snapshot] Nearest V_G: deterministic kinds (branches, charge_balance, vg_curve) are also recorded on a V_G grid (−3.8 … −0.9 V, 0.1 V, both presets; the illumination preset also at the measured V_G × P = 0/1.15/2.55/3.51 mW). A request that differs from a recorded one only in `device.vg` (and `device.light.power_mW` in power mode) gets the nearest point within 0.25 V / 0.8 mW (distance = ΔV_G/0.1 V + ΔP/1 mW) as a marked shallow copy (`snapshotApprox`); the panel says "nearest precomputed V_G = x V". Charge balance keeps `vd` in the signature: the UI derives it from the (nearest) folds, so it matches the grid point; a user-set V_D is a miss. Stochastic kinds and circuits never snap.
- [snapshot] Recorder: dev-only hook in `api/client.ts` (`import.meta.env.DEV && localStorage["stl-websim:record"] === "1"`; compiled out of builds) records `{kind, payload, key, near, result}` of every live job and the GET data responses into `window.__stlRecords`; `scripts/record-snapshot.mjs` drives a Vite dev server proxied to the backend through the default flows in KO and EN (grid once), dedupes by key and keeps the first label. Budget checks: ≤ 40 MB, ≤ 150 files.
- [snapshot] `web/snapshot/` and `web/dist-artifact/` are generated and gitignored (binary, tied to the engine version and to the exact frontend payloads; one command regenerates them: `npm run snapshot:record && npm run build:artifact`).
- [snapshot] Artifact build (`scripts/build-artifact.mjs`): `vite build --base ./`, snapshot copied to `snapshot/`, U+FFFD escaped in JS (`�`) and CSS, KaTeX `.woff`/`.ttf` deleted and every `@font-face` reduced to its woff2 source, entry `stl-simulator.html` = title + stylesheet/modulepreload links + `<div id="root">` + module script (no doctype/html/head/body), `files.json` lists the other files (`.gz` with contentType application/gzip); unreferenced top-level copies of `public/` (favicon) are dropped. `scripts/verify-artifact.mjs` serves the build like the host (wrapped entry, `/api` → 404) and checks it in Chromium.
- [snapshot] Demo backend (`api/mock.ts`): the HRS current is now built from components that follow the model qualitatively — GIDL ÷18 per +0.5 V of V_G (1.21e-13 A at V_D = 3 V, V_G = −2 V), subthreshold channel 10^(ΔV_G/(1.78·60 mV)) (9.9e-22 A), negligible junction BTBT, seed/impact ionisation growing near the fold — and V_LU(−1.5 V) ≈ 4.10 V; still labelled demo data everywhere.
- [axes-logo] Plot labels: every Plotly axis title, colorbar title, legend name, hover word and annotation in the result panels comes from the `axis.*` block of `web/src/i18n/strings.brand.ts` (KO/EN; `strings.ts` untouched). Format: LONG "Name symbol (unit)" (e.g. "Drain voltage V<sub>D</sub> (V)" / "드레인 전압 V<sub>D</sub> (V)") on every x-axis, single-panel y-axis and colorbar; SHORT "symbol (unit)" (`axis.s.*`, e.g. "σ<sub>LU</sub> (mV)", "U (k<sub>B</sub>T)") on the y-axes of vertically stacked subplots, where the legend names the quantity. Circuit signal axes hold many signals and have no single symbol, so their compact form is the name ("Voltage (V)", "Current |I| (A)", "State variable (V)", "Logic level"). Subscripts are Plotly HTML; units in parentheses, dimensionless quantities without; sentence case in English; KO keeps branch/fold/hazard in Latin script. A unit test (`web/src/plots/labels.test.ts`) checks both languages, no raw "X_Y" symbols and matching placeholders.
- [axes-logo] Units follow the data: current axes keep SI tick prefixes with an "A" suffix (log: one prefix per decade; linear: Plotly's shared engineering exponent, so all ticks share one prefix — the old d3 "~s" mixed 500nA/1µA on one linear axis); circuit voltage/charge/state axes use the same shared-SI ticks with the unit suffix (minexponent 1: 0.5V … but 20mV for mV-scale nodes); time scales to s/ms/µs/ns. Server-provided circuit labels ("V_LU (drain node)", "ΔQ_B") are converted to subscripts client-side (`plots/labels.ts: subs`); sweep symbols (V_amp, P_sw, V_G, I_PH, P(1)) map to named axis titles.
- [axes-logo] Design map: the log colour scale keeps the colorbar title (no "log₁₀ …") and labels the ticks with the values themselves (1-2-5 or decade ticks); hover shows the value, not its log. N_t reference labels shortened to "5×10¹²" etc. at −45° (they overlapped), the footer explains them (cm⁻²). Hazard: the sweep rate moved from the x title to an annotation in the survival strip; components panel: the followed branch ("HRS branch" / "Full locus") is an in-plot annotation instead of an "— HRS" suffix on the x title.
- [axes-logo] Stacked waveform viewers (quick benches, schematic results) grow with the number of subplots (≤ 2: 340 px, 3: 420, 4: 500, 5: 580 px; `circuit/axes.ts: stackClass`, CSS in `circuit/circuit.css`) so each axis keeps ≈ 80 px and its title fits. Plotly now uses `layout.autosize: true` (theme) and `PlotImpl` observes its container (ResizeObserver): a plot follows its container when the class changes or the sidebar toggles, not only on window resize (the schematic viewer previously kept a 340 px drawing inside a 420 px box).
- [axes-logo] Logo = the biristor symbol (owner's figure): NPN in a circle, collector up (C), emitter down with the outward arrow, base left open/floating (B) — drawn as SVG strokes (letters are stroked paths, no font) in white on the unchanged gradient badge (--logo-a/b/c, both themes). Detail follows the size: < 24 px heavier strokes (≈ 1.45×), larger circle, bare base bar (this is `public/favicon.svg`); 24–39 px adds the open base lead (stub + hollow terminal ring; header logo 34 px); ≥ 40 px adds the C/B letters (About card 56 px) — at 32 px the letters were not clean. Three variants were compared at 16/24/32/64/96 px on light and dark (full with ground symbol / two-terminal / bold compact); the two-terminal one was kept (the ground symbol turned to mush below 32 px). `BiristorGlyph` (monochrome, currentColor) marks the device row of the About card in --logo-b; the About header keeps the full-colour logo, a second glyph there would duplicate it.
- [snapshot] The recorder starts Vite through its JS API with `server.hmr: false` and file watching off, so edits to the source tree during a (30–40 min, cold cache) recording cannot hot-reload the page or abort running jobs; each step is retried once when it recorded nothing, and a failed grid point reopens the page.
- [oscillator] p-bit (owner request via orchestrator): new custom-netlist element `CMP` (comparator) = ideal inputs + behavioural voltage source out → ground, y = v_low + (v_high − v_low)(1 + tanh((V(in) − V(inm) − thr)/w))/2, w = 1 mV, thr = v_ref ± hysteresis/2 by the output state (updated per accepted step). Stamped as a V-source branch whose wave index is −1 − j (x layout and the benches' kernel paths unchanged); the output node is excluded from the node-voltage step control. ERC: output must not be tied to another source/comparator/ground. Firing windows = periods of the periodic pulse source with the most periods; fired = output high anywhere in the period (tracked at full resolution in the streaming sink); result key `comparators` + summary `CMPx.p_fire/lag1/n_bits/n_rise/duty`.
- [oscillator] Found while building the p-bit: the STL Jacobian had no ∂/∂V_GS terms, although V_GS = v_g − v_s is re-evaluated at every Newton iteration — harmless with a grounded source and DC gate (the benches), but a 100 kΩ source resistor (V_S jumps 0.44 V at latch-up) made Newton fail (time-step underflow). Cells whose V_GS can move (source not grounded or gate not held by a constant source) now get FD ∂/∂V_GS columns at the gate/source nodes; benches unchanged (flag off).
- [oscillator] Bench `pbit` replaced by the owner's topology: drain pulses (V_clk directly on the drain) → STL → R_S (100 kΩ) → ground, comparator on V(R_S) with v_ref (auto R_S × 1 µA = 0.1 V; latched ≈ 0.44 V, unlatched µV); bit = [R_S I_D > v_ref] at the end of the flat top; default pulse high V_LU − 15 mV (P1 ≈ 0.5 with carrier noise for the reference device, 0 deterministically). Keys `R_L_ohm`, `C_d_F`, `cmp_threshold_V` removed (ignored with the unknown-parameter warning); summary `v_th` now holds V_ref, new `R_S`. The old load-resistor variant was not kept (not trivial to keep both in the UI). This supersedes the pbit key list of the [frontend] entry above.
- [oscillator] Schematic p-bit example (replaces "p-bit with load resistor"): 20 × 3.69 V pulses, R_S 100 kΩ, CMP1 V_ref 0.1 V, t_stop 20 ms, stochastic local states overridden to "none" (carrier-noise randomness; the library's evolving GIDL states would make runs fire almost always or never). Editor: comparator part (shortcut K, pins IN/OUT, inspector V_ref/V_high/V_low/hysteresis), `CMPx.bit` default logic trace, results panel "Comparator firing" (run × pulse raster + P(fire) per pulse, P, lag-1); the demo mock switches comparators explicitly on the previous step.
- [param-guide] Parameter guide numbers (web/src/content/params/sensitivity.json, scripts/param_sensitivity.py) come from finite differences with the real engine at both presets, "paper" (reference: V_G −2 V, dark, 0.4 V/s, 0 → 4 V) and "photo" (illumination: V_G −1.8 V, 1200 V/s, γ 0.2794, δφ_G0 +74.4 mV, 0 → 5 V). Deterministic folds use classify_checked/folds_of at grid 601 with an increase/decrease step (×2/×0.5, ±10 mV, ±0.1 V, 0 → value …), a local slope from ±5 % or step/5, and extra points plus bisected window edges, where "locus gap" (fold outside the traceable range, a model limit) is kept apart from a real window closure (V_LU → V_LD). Stochastic parameters use run_sweep_mc (n = 200, fixed seed) or run_hazard. The ls_sigma reference block uses the general engine at 0 → 6 V so that no cycle is censored (at 0 → 4 V and σ ×2, 5 % of cycles never latch up, which biases the mean V_LU by −27 mV). The preset's 4 V lookup view is kept as auto_engine_4V, and `--only ls_sigma` re-runs just that block. guide.test.ts checks every arrow and magnitude against the JSON: |Δ| < 3 SE shows as "→", and magnitudes must fall within 6 %.
- [auth] Optional server-side password gate (`server/auth.py`, ASGI middleware between CORS and the body-size limit): off unless `STL_ACCESS_PASSWORD` is set; then only `/login` (GET page, POST form), `/logout`, `/api/health` and `/favicon.svg|.ico` are open — `/api/*` and `/openapi.json` and file-like paths answer 401 `{"error": "login required", "detail": …}` (the web client shows `detail`), page GET/HEAD requests (`/`, SPA routes, `/docs`, `/redoc`) get 303 → `/login?next=…`. The FastAPI docs stay enabled but behind the gate. Password check = PBKDF2-HMAC-SHA256 (200 000 iterations, per-process salt) + `hmac.compare_digest`; the plain password is not kept. Cookie `stl_session` = `v1.<iat>.<exp>.<nonce>.<HMAC-SHA256>` with key = HMAC(`STL_SESSION_SECRET` or 32 random bytes per process, PBKDF2(password)) — a password or secret change invalidates every cookie, and without the secret every restart does; HttpOnly, SameSite=Lax, Path=/, Secure on https (scope scheme or `X-Forwarded-Proto`); 30 days with "remember" (Max-Age), else a session cookie whose token expires after 24 h. Stateless: `/logout` clears the cookie in that browser only (sign everyone out = rotate `STL_SESSION_SECRET`); a `/logout` request marked `Sec-Fetch-Site: cross-site|same-site` gets a confirmation page (POST form) instead. `next` must be a same-site path (URL-safe ASCII, starts with one `/`, no scheme/host/backslash/whitespace, and not /login or /logout after browser-style dot-segment removal and percent-decoding, e.g. `/%6cogout`) or it becomes `/`. Limiter: every attempt is reserved as a failure before the check (parallel requests cannot bypass it), 5 failures → 30 s, doubling to 15 min (exponent capped), failures forgotten after an idle, unlocked hour; global window 20 failures / 60 s, and while it is full only clients without recent failures may try (≤ 10 grace checks / 60 s) — an attacker who fills the window cannot lock everyone out, and ≤ 30 checks/min run server-wide (a WARNING is logged when it trips). Client key = socket peer (IPv6 grouped by /64), or with `STL_TRUST_PROXY=N` the N-th `X-Forwarded-For` entry from the right; without it every request that carries `X-Forwarded-For` shares ONE key, because uvicorn with the Dockerfile's `--forwarded-allow-ips='*'` has already replaced the peer by the client-supplied left-most entry (rotating or naming addresses gives neither a bypass nor a targeted lock-out). Authenticated responses get `Cache-Control: public` rewritten to `private` so a CDN cannot serve assets/data to anyone. Login page: self-contained HTML (inline CSS, no JS, CSP `default-src 'none'`), KO with EN lines, light/dark by `prefers-color-scheme`, 390 px. Fail closed: `STL_REQUIRE_PASSWORD=1` without a (non-blank) password → every request incl. `/api/health` answers 503 (the host's health check fails) instead of an open site; the password value is stripped of surrounding whitespace (warning; also warns on surrounding quotes). The gate state is printed to stderr at start-up (`STL: access gate ON|OFF|MISCONFIGURED …`, visible under plain uvicorn) and `/api/health` reports `access_gate`. `render.yaml` declares `STL_ACCESS_PASSWORD` with `sync: false` (prompted only when a blueprint is first applied), sets `STL_REQUIRE_PASSWORD=1`, generates `STL_SESSION_SECRET` (`generateValue`) and sets `STL_TRUST_PROXY=1`. The gate does not apply to the static snapshot artifact (no server).
- [lock] Password-locked artifact (`npm run build:artifact -- --lock`, password only from `STL_ARTIFACT_PASSWORD`; `scripts/lock-build.mjs`, `lock-crypto.mjs`, `lock/`): the claude.ai host has no server side, so the page is protected by encryption, not access control. File format `"STLENC1\0"` · 12-byte random IV · AES-256-GCM ciphertext‖tag, AAD = the published path (a file cannot be swapped for another); key = PBKDF2-HMAC-SHA256(NFC password, random 16-byte salt, 600 000 iterations — OWASP 2023; `--iterations` raises it), salt/iterations/file names in plaintext `lock.json`. The whole app is a second Vite build through the JS API (`vite.config.ts` unchanged, `build.write: false`, `format: "iife"`, `codeSplitting: false` = Rolldown's inlineDynamicImports, `cssCodeSplit: false`, no source maps) → gzip → encrypted `assets/app-<random>.wasm`; every snapshot file and the index (presets/meta inside) → gzip → encrypted `snapshot/<random>.bin` + `snapshot/index.wasm`, `application/octet-stream` (a binary `.json` would trip the publisher's UTF-8 text checks). The app stylesheet is encrypted too (`assets/style-<random>.wasm`) and applied after unlock as a constructable stylesheet (CSSOM, not subject to `style-src`), falling back to `<style>` then a `blob:` link; relative `url()`s are made absolute first. Plaintext: the card (entry fragment, `lock.js`, `lock.css`; no credit line — lab/people names are password-guessing vocabulary, the credit shows inside the app), `lock.json` and the stock KaTeX woff2 fonts (must be byte-identical to `node_modules/katex/dist/fonts`); the build scans the text files for model strings (GIDL, calib*, impact, latch as a substring, device/model words, V_LU/V_LD, parameter names, fold voltages, the illumination γ, 6-digit fingerprints of the presets' calibrated numbers, lab and people names) and every output file for the password (raw/NFC/base64), and decrypts every file once (and checks that a different AAD fails). It writes to a temporary sibling folder and replaces the output folder only when all of that passes (a failed build leaves nothing publishable). A password with leading/trailing whitespace or control characters is refused; `verify:artifact` reads it only from the environment (no `--password`). Loader: `lock.json` → the app `.wasm` downloads while the password is typed → PBKDF2 (Web Crypto) → decrypt → `globalThis.__STL_LOCK__ = { key }` (non-extractable AES key, non-enumerable) → run the IIFE with the first method the page's CSP allows: `<script src=blob:>` (onload/onerror), inline `<script>` (runs synchronously or silently not at all), `new Function`/indirect eval; created scripts copy `lock.js`'s nonce (nonce and `'strict-dynamic'` CSPs work); a `__STL_RAN__` marker prepended to the code separates "blocked" (try the next method) from "ran and threw" (report, never run twice); success = `__STL_BOOTED__` set at the top of `main.tsx`; none → bilingual CSP error in the card. "Remember" stores the derived raw key (never the password) as {k, exp} in localStorage["stl-lock:<salt>"] for 30 days, otherwise in sessionStorage (1 day); keys of older salts and expired or damaged keys are removed; a failed password with surrounding whitespace is retried trimmed; `#lock` in the URL (at load or while the app runs) or `__STL_LOCK__.lock()` forgets every saved key on that browser. The app's own localStorage entries (`stl-websim:*`, which hold the preset's calibrated parameters) must not sit on disk in plaintext on a locked page, and the store files belong to the UI: the gate therefore patches `Storage.prototype.getItem/setItem/removeItem` for `localStorage` + `stl-websim:*` only, serves them from memory and saves them as one AES-GCM entry `stl-lock-state:<salt>` (HKDF-SHA256 subkey of the page key, AAD `state:<salt>`); plaintext `stl-websim:*` entries (e.g. from an earlier plain build of the page) are removed before any unlock and carried into the encrypted state after it. `api/snapshot.ts`: one reader `readSnapshotBytes` (magic → decrypt with the gate's key, AAD = the fetched page-relative path, which is the published path; gzip magic → inflate; JSON.parse) for the index and every file; with a key it loads `index.wasm` instead of `index.json`; plain builds and dev never see encrypted bytes. `verify:artifact` detects `lock.json` and checks: static leak/password scan, wrong password (error, app never runs, no data fetched), right password (V_LU 3.704 / V_LD 2.598 from decrypted snapshot files), session vs remembered key, damaged key, keyboard-only unlock, 390 px light/dark/host data-theme, and a CSP matrix (base `default-src 'self'` … + only blob: / only 'unsafe-inline' / only 'unsafe-eval' / only a nonce / nonce + 'strict-dynamic' must boot; none → the error, no blank page). Protection = password strength (offline guessing against `lock.json` + any `.wasm` is possible); the claude.ai link must still be shared by the owner.
